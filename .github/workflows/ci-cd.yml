
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Run a fake command
        run: |
          echo "This will fail"
          exit 1  # force fail
